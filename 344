class Solution(object):
    def reverseString(self, s):
        """
        :type s: str
        :rtype: str
        """
        """result=''
        for i in range(0,len(s)):
            result+=s[len(s)-1-i]
        return result"""
        return s[::-1]
